{
  "name": "suwappu-trading-bot",
  "version": "2.0.0",
  "description": "Standalone preview-first price-target workflow with outcome-safe Suwappu managed execution",
  "private": true,
  "type": "module",
  "bin": { "suwappu-trading-bot": "src/cli.ts" },
  "scripts": {
    "start": "bun src/cli.ts --once",
    "watch": "bun src/cli.ts",
    "dev": "bun --watch src/cli.ts",
    "executions": "bun src/cli.ts executions",
    "check": "tsc --noEmit",
    "test": "bun test",
    "build": "bun build src/cli.ts --outdir dist --target bun",
    "verify": "bun run check && bun test && python -m py_compile bot.py && python -m unittest discover -s tests -p 'test_*.py' -v && bun run build && bun audit --audit-level=high"
  },
  "dependencies": {
    "commander": "12.1.0"
  },
  "devDependencies": {
    "@types/bun": "1.4.0",
    "typescript": "5.9.3"
  },
  "engines": { "bun": ">=1.3.14" },
  "packageManager": "bun@1.3.14",
  "license": "MIT"
}
