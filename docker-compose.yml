services:
  bot:
    build: .
    env_file: .env
    volumes:
      - bot-state:/data
    # Do not put a money-moving command behind an automatic restart policy.
    restart: "no"

volumes:
  bot-state:
